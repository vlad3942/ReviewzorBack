spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reviewzor
    username: postgres
    password: P@$$w0rd
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate.ddl-auto: update
    #database-platform: org.hibernate.dialect.MySQL5Dialect
